# Core GUI
PyQt5

# TTS backends
edge-tts          # primary: 400+ neural voices via Microsoft Edge TTS
pyttsx3           # offline fallback using system TTS engine
chatterbox-tts    # Chatterbox voice cloning — Python 3.10+, ~400 MB model on first use
# TTS            # Coqui XTTS v2 voice cloning — requires Python 3.9–3.11 only
                  # Install manually on Python 3.9–3.11: pip install TTS
                  # On Python 3.12+ this package is unavailable; Chatterbox is used instead

# Audio processing
pydub             # manipulation, trim, speed, mp3/wav export
librosa           # audio analysis (gender detection via pitch)
soundfile         # WAV I/O
numpy
scipy

# Language detection
langdetect

# Audio metadata
mutagen

# Build tools (Windows packaging)
setuptools
wheel
