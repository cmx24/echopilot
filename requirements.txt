# Core GUI
PyQt5

# TTS backends
edge-tts          # primary: 400+ neural voices via Microsoft Edge TTS
pyttsx3           # offline fallback using system TTS engine
# Voice cloning backends — install manually (see setup.bat):
#   chatterbox-tts  → Python 3.10–3.11 only (numpy 1.24-1.25 required; no wheels for 3.12)
#                     pip install chatterbox-tts
#   TTS             → Coqui XTTS v2, Python 3.9–3.11 only
#                     pip install TTS

# Audio processing
pydub             # manipulation, trim, speed, mp3/wav export
librosa           # audio analysis (gender detection via pitch)
soundfile         # WAV I/O
numpy
scipy

# Language detection
langdetect

# Audio metadata
mutagen

# Build tools (Windows packaging)
setuptools
wheel
