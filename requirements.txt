# Core GUI
PyQt5

# TTS backends
edge-tts          # primary: 400+ neural voices via Microsoft Edge TTS
pyttsx3           # offline fallback using system TTS engine
TTS               # Coqui XTTS v2 — zero-shot voice cloning from reference audio
                  # (~2 GB model download on first use; skip if not needed)

# Audio processing
pydub             # manipulation, trim, speed, mp3/wav export
librosa           # audio analysis (gender detection via pitch)
soundfile         # WAV I/O
numpy
scipy

# Language detection
langdetect

# Audio metadata
mutagen

# Build tools (Windows packaging)
setuptools
wheel
